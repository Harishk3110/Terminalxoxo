# Limitations

Version 0.1 is a working demo-mode scaffold. It does not yet include full provider backfills, persistent database migrations, exhaustive E2E coverage, production SSO, or live external data credentials.

No feature should be treated as connected-provider complete until configured and tested with real credentials.
