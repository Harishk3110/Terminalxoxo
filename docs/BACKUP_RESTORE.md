# Backup and Restore

Backups cover PostgreSQL dumps, object-storage inventories, generated reports, and configuration exports without secrets.

Commands:

- `make backup`
- `make restore`
- `make verify-backup`
