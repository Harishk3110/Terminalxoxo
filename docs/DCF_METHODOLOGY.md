# DCF Methodology

DCF outputs require explicit revenue, margin, reinvestment, tax, WACC, terminal value, net debt, share count, and sensitivity assumptions.

Unavailable provider fields must remain unavailable unless the user explicitly marks them as estimates.
