# Data Drop

Data Drop accepts permitted CSV, XLSX, PDF, and screenshot attachments. The initial fixture is `tests/fixtures/demo-upload.csv`.

Imports require column mapping, validation, quality state, provider/source notes, and quarantine for failed rows.
