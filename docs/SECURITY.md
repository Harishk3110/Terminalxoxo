# Security

Security controls include password hashing, TOTP setup path, secure cookies, CORS allowlists, security headers, rate-limiting plan, audit logs, provider-secret redaction, and dependency scanning.

The initial build includes source scanning for forbidden broker action method names.
