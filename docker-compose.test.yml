services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: knk_terminal_test
      POSTGRES_USER: knk
      POSTGRES_PASSWORD: knk_test
    ports:
      - "127.0.0.1:55432:5432"

  redis-test:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:56379:6379"
