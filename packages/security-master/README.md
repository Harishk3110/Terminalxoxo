# @knk/security-master

Reserved TypeScript package for instrument identity, listings, exchanges, identifiers, provider mappings, and corporate-action helpers.

The operational instrument master currently lives in the API database (`instruments`, `instrument_identifiers`, `exchanges`, and `provider_instrument_mappings`) and is exposed through `/api/v1/instruments`.
